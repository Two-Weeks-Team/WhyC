// Cron: public_stats_rebuild — nightly.
// Aggregates Run + Iteration tables into a single PublicStatsSnapshot row.
// STUB. TODO(WK2): implement once pipeline-kickoff produces real Run rows.
export async function run(): Promise<void> {
  console.warn('[public-stats-rebuild] STUB — not implemented yet (WK2 deliverable)');
}
