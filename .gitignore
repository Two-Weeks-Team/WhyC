# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node
node_modules/
.next/
dist/
build/
out/
.turbo/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# Build artifacts / caches
.cache/
coverage/
.nyc_output/

# Claude Code local-only state (workspace settings.local.json IS committed for collaborator parity)
.claude/cache/
.claude/logs/

# pf run scratch (we keep the design-locked run; ignore future scratch)
runs/.tmp/
runs/*.tmp/

# OS misc
Thumbs.db
ehthumbs.db

# Pipeline run artifacts — the committed r-20260506T122526Z run is design
# provenance (referenced by the master plans); new pipeline runs are ephemeral.
/runs/r-*/
!/runs/r-20260506T122526Z/
# pf-plugin scratch
/runs/.last-check.*
/data/yc-candidates.json
/data/dataset-evidence.jsonl
